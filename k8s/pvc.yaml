# Persistent Volume Claim for Adjutant data (config, tokens, logs)
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: calendar-mcp-data
  namespace: calendar-mcp
  labels:
    app: calendar-mcp
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
